[pytest]
# Fast, isolated, in-process tests (FastAPI TestClient + the SQLite engine).
# Each test gets a fresh temp database, so no shared state and no xdist needed.
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
